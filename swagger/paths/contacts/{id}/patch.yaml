tags:
  - Contacts
summary: Update contact
operationId: updateContact
description: 'Update a contact. Only the owner (userId) can update.'
security:
  - bearerAuth: []
parameters:
  - in: path
    name: id
    required: true
    schema:
      type: string
      example: '65ca67e7ae7f10c88b598384'
requestBody:
  content:
    multipart/form-data:
      schema:
        type: object
        properties:
          name:
            description: "Contact's name"
            type: string
            example: 'John Doe'
          phoneNumber:
            description: "Contact's phone number in international format"
            type: string
            example: "+380931112233"
          email:
            description: "Contact's email address"
            type: string
            example: "example@email.com"
          isFavourite:
            description: "Indicates whether the contact is marked as favourite"
            type: boolean
            example: false
          contactType:
            description: "Type of contact, can be 'work', 'home', or 'personal'"
            type: string
            enum:
            - work
            - home
            - personal
            example: "personal"
          userId:
            description: "ID of the user who owns this contact"
            type: string
            example: 65e4decdd286b30065d54af9
          photo:
            description: "Contact's photo"
            type: string
            format: binary
responses:
  '200':
    description: Successfully updated a contact
    content:
      application/json:
        schema:
          type: object
          required:
            - status
            - message
            - data
          properties:
            status:
              type: integer
              example: 200
            message:
              type: string
              example: Successfully patched a contact!
            data:
              type: object
              $ref: '../../../components/schemas/contact.yaml'
  '401':
    $ref: '../../../components/responses/401.yaml'
  '400':
    $ref: '../../../components/responses/400.yaml'
  '404':
    $ref: '../../../components/responses/404.yaml'
